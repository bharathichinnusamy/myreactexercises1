//import react into the bundle
import React from "react";
import ReactDOM from "react-dom";
import PropTypes from "prop-types";
var output = (
	<p>
		James is <strong>12</strong>
		years old
	</p>
);

ReactDOM.render(output, document.querySelector("#app"));
